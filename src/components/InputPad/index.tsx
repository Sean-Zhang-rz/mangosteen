import { defineComponent } from 'vue';
import styles from './index.module.scss';

export const InputPad = defineComponent({
  setup: (props, context) => {
    return () => <div>InputPad</div>;
  },
});
